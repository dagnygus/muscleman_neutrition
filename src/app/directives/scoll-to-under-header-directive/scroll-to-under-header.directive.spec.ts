import { ScrollToUnderHeaderDirective } from './scroll-to-under-header.directive';

describe('ScrollToUnderHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollToUnderHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
