import { DnngOnDestroyDirective } from './dnng-on-destroy.directive';

describe('DnngOnDestroyDirective', () => {
  it('should create an instance', () => {
    const directive = new DnngOnDestroyDirective();
    expect(directive).toBeTruthy();
  });
});
