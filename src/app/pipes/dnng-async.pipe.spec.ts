import { DnngAsyncPipe } from './dnng-async.pipe';

describe('DnngAsyncPipe', () => {
  it('create an instance', () => {
    const pipe = new DnngAsyncPipe();
    expect(pipe).toBeTruthy();
  });
});
