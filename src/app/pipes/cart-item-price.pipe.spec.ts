import { CartItemPricePipe } from './cart-item-price.pipe';

describe('CartItemPricePipe', () => {
  it('create an instance', () => {
    const pipe = new CartItemPricePipe();
    expect(pipe).toBeTruthy();
  });
});
